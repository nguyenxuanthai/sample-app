<%= link_to gravatar_for(current_user, size: Settings.user_info.gravatar_size),
  current_user %>
<h1><%= current_user.name %></h1>
<span>
  <%= link_to t("app.views.shared.user_info.my_profile"), current_user %>
</span>
<span>
  <%= pluralize(current_user.microposts.count,
    t("app.views.shared.user_info.micropost")) %>
</span>
