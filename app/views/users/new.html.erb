<% provide :title, t("view.new.title") %>
<h1><%= t "view.new.sign_up" %></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for @user do |f| %>
      <%= render "form", user: @user %>
    <% end %>
  </div>
</div>
